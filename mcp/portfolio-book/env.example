# portfolio-book has no external dependencies or secrets. BOOK_STORE_PATH
# (default /data/portfolio-book.json) is where it keeps its records; on first
# start it seeds them from the bundled book.json.
