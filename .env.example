# Only the provider this app actually uses. AgentBox overwrites these with
# SecureProxy's virtual key and base URL at `app add` time; they matter only
# when running the app outside AgentBox.
OPENAI_API_KEY=

# Optional.
OPENAI_MODEL=gpt-5.2
